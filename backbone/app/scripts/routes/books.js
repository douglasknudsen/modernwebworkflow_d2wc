/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var BooksRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return BooksRouter;
});